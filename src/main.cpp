#include <bits/stdc++.h>
#include "chess_engine/board.h"

using namespace std;

int main() {
    std::cout << "Welcome to AI-Based Chess Engine!" << std::endl;

    // Initialize the chess board
    Board board;
    board.display();

    return 0;
}